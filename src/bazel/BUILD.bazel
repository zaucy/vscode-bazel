load("//:macros.bzl", "ts_library")

ts_library(
    name = "bazel",
    visibility = ["//visibility:public"],
    srcs = glob(["*.ts"]),
    deps = [
        "//src/protos",
        "//src/extension:configuration",
        "@npm//@types/node",
        "@npm//vscode",
    ],
)
