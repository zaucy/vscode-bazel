load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = "buildifier",
    visibility = ["//visibility:public"],
    srcs = glob(["*.ts"]),
    deps = [
        "@npm//@types/node",
        "@npm//vscode",
        "@npm//@types/which",
        "@npm//which",
    ],
)
