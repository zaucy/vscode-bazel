load("//:macros.bzl", "ts_library")

ts_library(
    name = "buildifier",
    visibility = ["//visibility:public"],
    srcs = glob(["*.ts"]),
    deps = [
        "@npm//@types/node",
        "@npm//vscode",
        "@npm//@types/which",
        "@npm//which",
    ],
)
